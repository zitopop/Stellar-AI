# Settings v4

`stellar-settings-v4.css` is the authoritative desktop Settings presentation layer.
It is loaded by `currency.js` on `/app` so it can override legacy inline Settings styles without changing app logic.

Do not add additional desktop Settings overrides to `app.html`.
