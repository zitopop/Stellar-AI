import { writeFileSync } from 'node:fs';

const rows = ['# Stellar AI — 400-task progress tracker', '', '> This tracker counts implementation tasks, not time. A task becomes complete only after its code or documentation is finished and the relevant tests/checks pass.', '', '| Task | Status | Scope | Evidence | Checkpoint |', '|---:|:---:|---|---|---|'];
for (let task = 1; task <= 400; task += 1) {
  if (task === 1) rows.push('| 1 | ✅ Complete | Multi-AI provider contract, role routing, plan gating, and Anthropic-compatible SSE envelope | `tests/multi-model-routing.test.mjs`; 20 Stellar AI tests pass | `f68195f` |');
  else if (task === 2) rows.push('| 2 | ✅ Complete | Retryable built-in-provider failure falls back once to the existing Anthropic tier | `tests/multi-model-routing.test.mjs`; 21 Stellar AI tests pass | `b9085b5` |');
  else if (task === 3) rows.push('| 3 | ✅ Complete | Roblox, FiveM, mixed, and general platform-aware quality gates | `tests/multi-model-routing.test.mjs`; 24 Stellar AI tests pass | `7023cac` |');
  else if (task === 4) rows.push('| 4 | ✅ Complete | Roblox Build Pack, FiveM resource, audit, and general workflow-mode contracts | `tests/multi-model-routing.test.mjs`; full suite 27 tests pass | `063b459` |');
  else if (task === 5) rows.push('| 5 | ✅ Complete | QBCore, ESX, ox_lib, standalone, and conflicting-framework safety context | `tests/multi-model-routing.test.mjs`; full suite 30 tests pass | `c714b87` |');
  else if (task === 6) rows.push('| 6 | ✅ Complete | Mobile plans-modal dynamic scrolling, overscroll containment, and safe-area padding | `app.html`; 30 tests, phone/tablet screenshots, static smoke checks | `e6c8fe6` |');
  else if (task === 7) rows.push('| 7 | ✅ Complete | Provider-specific GPT, Claude, Gemini, and unknown-model token/reasoning contract | `tests/multi-model-routing.test.mjs`; full suite 34 tests pass | `2060de9` |');
  else if (task === 8) rows.push('| 8 | ✅ Complete | Planner, implementer, researcher, security, and tester output contracts | `tests/multi-model-routing.test.mjs`; full suite 36 tests pass | `f3ffa6f` |');
  else if (task === 9) rows.push('| 9 | ✅ Complete | Truthful specialist-role descriptions aligned to deployed research, security, and tester contracts | `app.html`; full suite 36 tests, phone/tablet captures, static smoke checks | `4ecdf6c` |');
  else if (task === 10) rows.push('| 10 | ✅ Complete | Strict security JSON Schema response contract for Forge-routed reviews | `tests/multi-model-routing.test.mjs`; full suite 37 tests pass | `3a8bb0d` |');
  else if (task === 11) rows.push('| 11 | ✅ Complete | Strict planner and tester JSON Schema response contracts | `tests/multi-model-routing.test.mjs`; full suite 38 tests pass | `b1ff6d6` |');
  else if (task === 12) rows.push('| 12 | ✅ Complete | Strict researcher JSON Schema with claim-level provenance and uncertainty fields | `tests/multi-model-routing.test.mjs`; full suite 39 tests pass | `e86cb32` |');
  else if (task === 13) rows.push('| 13 | ✅ Complete | Strict implementer JSON Schema for complete destination-labelled file bundles | `tests/multi-model-routing.test.mjs`; full suite 40 tests pass | `7b47803` |');
  else if (task === 14) rows.push('| 14 | ✅ Complete | Wire-level forwarding test for all role schemas and stream compatibility | `tests/multi-model-routing.test.mjs`; full suite 41 tests pass | `7212e8d` |');
  else if (task === 15) rows.push('| 15 | ✅ Complete | Fallback test preserving FiveM QBCore role, platform, workflow, and framework guidance | `tests/multi-model-routing.test.mjs`; full suite 42 tests pass | `08c3ce6` |');
  else if (task === 16) rows.push('| 16 | ✅ Complete | Mixed Roblox/FiveM isolation and conservative framework ambiguity regression coverage | `tests/multi-model-routing.test.mjs`; full suite 43 tests pass | `e6fd77d` |');
  else if (task === 17) rows.push('| 17 | ✅ Complete | Structured-output fallback transparency notice and regression coverage | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 44 tests pass | `68e3708` |');
  else if (task === 18) rows.push('| 18 | ✅ Complete | Retryable Forge network-error normalization with existing Anthropic fallback | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 45 tests pass | `a07007e` |');
  else if (task === 19) rows.push('| 19 | ✅ Complete | AbortError propagation without fallback request | `tests/multi-model-routing.test.mjs`; full suite 46 tests pass | `518ddc7` |');
  else if (task === 20) rows.push('| 20 | ✅ Complete | Malformed Forge JSON normalization with existing Anthropic fallback | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 47 tests pass | `febcb15` |');
  else if (task === 21) rows.push('| 21 | ✅ Complete | Anthropic candidate retry after non-abort network error | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 48 tests pass | `cf173d5` |');
  else if (task === 22) rows.push('| 22 | ✅ Complete | Direct Anthropic AbortError propagation without trying another candidate | `tests/multi-model-routing.test.mjs`; full suite 49 tests pass | `559164f` |');
  else if (task === 23) rows.push('| 23 | ✅ Complete | Terminal Anthropic failure returns retryable 503 after all candidates fail | `tests/multi-model-routing.test.mjs`; full suite 50 tests pass | `dfc85c3` |');
  else if (task === 24) rows.push('| 24 | ✅ Complete | Direct Anthropic 404 fallback preserves the configured candidate stream | `tests/multi-model-routing.test.mjs`; full suite 51 tests pass | `9ea039e` |');
  else if (task === 25) rows.push('| 25 | ✅ Complete | Caller AbortSignal forwarded unchanged to Anthropic fetch | `tests/multi-model-routing.test.mjs`; full suite 52 tests pass | `810c531` |');
  else if (task === 26) rows.push('| 26 | ✅ Complete | Caller AbortSignal forwarded unchanged to Forge fetch | `tests/multi-model-routing.test.mjs`; full suite 53 tests pass; included in green production deployment `c27fc10` | `1864dd0` |');
  else if (task === 27) rows.push('| 27 | ✅ Complete | Forge 200 response without a usable completion falls back to the existing Anthropic stream | `tests/multi-model-routing.test.mjs`; full suite 54 tests pass; green Vercel deployment | `6cd0d73` |');
  else if (task === 28) rows.push('| 28 | ✅ Complete | Whitespace-only Forge completion is normalized to retryable empty-output failure and falls back to Anthropic | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 55 tests pass; green Vercel deployment | `2a69944` |');
  else if (task === 29) rows.push('| 29 | ✅ Complete | Forge-to-Anthropic fallback preserves the complete specialist system prompt unchanged | `tests/multi-model-routing.test.mjs`; full suite 56 tests pass; green Vercel deployment | `4793027` |');
  else if (task === 30) rows.push('| 30 | ✅ Complete | Successful Anthropic response without a stream body retries the configured candidate fallback | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 57 tests pass; green Vercel deployment | `d8add6b` |');
  else if (task === 31) rows.push('| 31 | ✅ Complete | Transient Anthropic upstream statuses retry the configured candidate fallback | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 58 tests pass; green Vercel deployment | `2228470` |');
  else if (task === 32) rows.push('| 32 | ✅ Complete | Permanent Anthropic authorization errors return unchanged without a fallback candidate request | `tests/multi-model-routing.test.mjs`; full suite 59 tests pass; green Vercel deployment | `fa4053d` |');
  else if (task === 33) rows.push('| 33 | ✅ Complete | Anthropic candidate 400 response advances to the configured fallback stream | `tests/multi-model-routing.test.mjs`; full suite 60 tests pass; green Vercel deployment | `5e0cde9` |');
  else if (task === 34) rows.push('| 34 | ✅ Complete | Non-finite Forge token inputs normalize to a valid safe minimum | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 61 tests pass; green Vercel deployment | `1a66ccb` |');
  else if (task === 35) rows.push('| 35 | ✅ Complete | Recognised specialist role input normalizes before applying its model, contract, and schema | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 62 tests pass; green Vercel deployment | `fe03380` |');
  else if (task === 36) rows.push('| 36 | ✅ Complete | Unknown specialist role input safely defaults to the complete implementer route and schema | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 63 tests pass; green Vercel deployment | `315f245` |');
  else if (task === 37) rows.push('| 37 | ✅ Complete | No-role routes preserve the requested model while retaining the implementer contract | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 64 tests pass; green Vercel deployment | `da66a6a` |');
  else if (task === 38) rows.push('| 38 | ✅ Complete | Inherited object-property names cannot select specialist routing roles | `api/chat.js`; `tests/multi-model-routing.test.mjs`; full suite 65 tests pass; green Vercel deployment | `8000e4f` |');
  else if (task === 39) rows.push('| 39 | ✅ Complete | Saved-tool favorite controls reach a 44px mobile touch target while desktop density remains unchanged | `index.html`; full suite 65 tests pass; live CSS rule and green Vercel deployment verified | `aefdb03` |');
  else if (task === 40) rows.push('| 40 | ✅ Complete | Mobile recording control reaches a 44px touch target without changing recording behavior | `app.html`; full suite 65 tests pass; live CSS rule and green Vercel deployment verified | `4013df8` |');
  else if (task === 41) rows.push('| 41 | ✅ Complete | CDN cache policy for public HTML routes only; API, auth, payments, secrets, and checkout routes remain unchanged | Full suite 65 tests pass; HTML/config checks pass; Vercel deployment `b8832c9` READY; live `x-vercel-cache: HIT` verified | `b8832c9` |');
  else if (task === 42) rows.push('| 42 | ✅ Complete | Message normalization filters blank or malformed records before retaining the newest 40 valid entries | `api/chat.js`; focused suite 51 tests and full suite 66 tests pass; Vercel deployment `0d1c906` READY | `0d1c906` |');
  else if (task === 43) rows.push('| 43 | ✅ Complete | Image attachments allow only bounded PNG, JPEG, GIF, or WebP base64 payloads before provider request construction | `api/chat.js`; focused suite 52 tests and full suite 67 tests pass; Vercel deployment `8d8dce4` READY | `8d8dce4` |');
  else if (task === 44) rows.push('| 44 | ✅ Complete | Chat-history normalization scans only the latest 400 raw records before retaining the newest 40 valid messages | `api/chat.js`; focused suite 53 tests and full suite 68 tests pass; Vercel deployment `0d4fe89` READY | `0d4fe89` |');
  else if (task === 45) rows.push('| 45 | ✅ Complete | Image attachment validation rejects structurally invalid base64 padding or length before provider payload construction | `api/chat.js`; focused suite 53 tests and full suite 68 tests pass; Vercel deployment `b248fa5` READY | `b248fa5` |');
  else if (task === 46) rows.push('| 46 | ✅ Complete | Forge multipart completions emit only string text parts; unusable content triggers the existing fallback instead of leaking object text | `api/chat.js`; focused suite 54 tests and full suite 69 tests pass; Vercel deployment `7d482fe` READY | `7d482fe` |');
  else if (task === 47) rows.push('| 47 | ✅ Complete | Regression coverage confirms ordered Forge multipart text streams normally and does not invoke fallback | `tests/multi-model-routing.test.mjs`; focused suite 55 tests and full suite 70 tests pass; Vercel deployment `e3488f2` READY | `e3488f2` |');
  else if (task === 48) rows.push('| 48 | ✅ Complete | Regression coverage confirms a non-text Forge single content object triggers fallback instead of user-visible object text | `tests/multi-model-routing.test.mjs`; focused suite 56 tests and full suite 71 tests pass; Vercel deployment `aa4d6e0` READY | `aa4d6e0` |');
  else if (task === 49) rows.push('| 49 | ✅ Complete | Workspace exposes a validated PNG, JPEG, GIF, or WebP image picker with preview, removal, and mobile touch-safe control | `app.html`; upload contract test and full suite 72 tests pass; Vercel deployment `9e855e3` READY | `9e855e3` |');
  else if (task === 50) rows.push('| 50 | ✅ Complete | Combined normalized chat text and attached image data are bounded before routing or provider payload construction | `api/chat.js`; focused suite 57 tests and full suite 73 tests pass; Vercel deployment `91f45e6` READY | `91f45e6` |');
  else if (task === 51) rows.push('| 51 | ✅ Complete | Supported clipboard images enter the existing validated workspace preview and removal flow without intercepting ordinary text paste | `app.html`; attachment contract and full suite 73 tests pass; Vercel deployment `6fa5f55` READY; live in-memory clipboard check verified | `6fa5f55` |');
  else if (task === 52) rows.push('| 52 | ✅ Complete | An unusable clipboard image item now falls through to the existing text-paste path instead of swallowing pasted content | `app.html`; attachment contract and full suite 73 tests pass; Vercel deployment `076e492` READY; live source and native long-text paste path verified | `076e492` |');
  else if (task === 53) rows.push('| 53 | ✅ Complete | Forwarded client identity is bounded and normalized before chat rate-limit key construction, preventing blank or oversized proxy values from producing unstable keys | `api/chat.js`; focused suite 58 tests and full suite 74 tests pass; Vercel deployment `57ad63a` READY | `57ad63a` |');
  else if (task === 54) rows.push('| 54 | ✅ Complete | Only valid IPv4 or IPv6 identities reach chat rate-limit key construction; malformed values, ports, and control characters use the shared unknown key | `api/chat.js`; focused suite 59 tests and full suite 75 tests pass; Vercel deployment `39bc2b9` READY | `39bc2b9` |');
  else if (task === 55) rows.push('| 55 | ✅ Complete | Untrusted search context is bounded before processing and stripped of non-text control characters before system-prompt construction | `api/chat.js`; focused suite 60 tests and full suite 76 tests pass; Vercel deployment `67af968` READY | `67af968` |');
  else if (task === 56) rows.push('| 56 | ✅ Complete | Individual chat-message content is capped before trimming and normalization, preserving recent valid messages while bounding per-record work | `api/chat.js`; focused suite 61 tests and full suite 77 tests pass; Vercel deployment `1e5c8d3` READY | `1e5c8d3` |');
  else if (task === 57) rows.push('| 57 | ✅ Complete | Supported image files can be dropped onto the workspace composer through the existing validation, preview, removal, and sending path without intercepting text-only drops | `app.html`; attachment contract and full suite 77 tests pass; Vercel deployment `347cac4` READY; live drop preview verified | `347cac4` |');
  else if (task === 58) rows.push('| 58 | ✅ Complete | Latest workspace image action wins: stale FileReader completions cannot overwrite a newer selection or a removed attachment | `app.html`; attachment contract and full suite 77 tests pass; Vercel deployment `596efa2` READY | `596efa2` |');
  else if (task === 59) rows.push('| 59 | ✅ Complete | Image payloads without a valid user message are rejected rather than silently omitted from provider input | `api/chat.js`; focused suite 62 tests and full suite 78 tests pass; Vercel deployment `38e982a` READY | `38e982a` |');
  else if (task === 60) rows.push('| 60 | ✅ Complete | Image attachments must align with the latest normalized user turn and cannot be associated with stale history | `api/chat.js`; focused suite 63 tests and full suite 79 tests pass; Vercel deployment `09e94af` READY | `09e94af` |');
  else if (task === 61) rows.push('| 61 | ✅ Complete | Image attachments must have decoded PNG, JPEG, GIF, or WebP signatures that match their declared media type before provider payload construction | `api/chat.js`; focused suite 64 tests and full suite 80 tests pass; Vercel deployment `e88c652` READY | `e88c652` |');
  else if (task === 62) rows.push('| 62 | ✅ Complete | Image data is serialized as a provider-format image part only on the final user payload entry, leaving earlier turns and source history unchanged | `api/chat.js`; focused suite 65 tests and full suite 81 tests pass; Vercel deployment `e77eca6` READY | `e77eca6` |');
  else if (task === 63) rows.push('| 63 | ✅ Complete | Image attachments require essential PNG, JPEG, GIF, or WebP container structure and reject header-only or truncated payloads before provider routing | `api/chat.js`; focused suite 66 tests and full suite 82 tests pass; Vercel deployment `5b17e5a` READY | `5b17e5a` |');
  else if (task === 64) rows.push('| 64 | ✅ Complete | Image attachments reject malformed primary PNG, JPEG, GIF, or WebP structures that otherwise pass simple signature and trailer checks | `api/chat.js`; focused suite 67 tests and full suite 83 tests pass; Vercel deployment `0b195e5` READY | `0b195e5` |');
  else if (task === 65) rows.push('| 65 | ✅ Complete | Client-provided model and role strings are limited to 128 normalized characters and non-string values safely default before chat route resolution | `api/chat.js`; focused suite 68 tests and full suite 84 tests pass; Vercel deployment `2b7335b` READY | `2b7335b` |');
  else if (task === 66) rows.push('| 66 | ✅ Complete | Handler-level regression coverage proves the existing chat timeout and disconnect listener are cleaned up after streamed success and upstream error paths | `tests/multi-model-routing.test.mjs`; focused suite 70 tests and full suite 86 tests pass; Vercel deployment `36c6219` READY | `36c6219` |');
  else if (task === 67) rows.push('| 67 | ✅ Complete | Workspace composer preserves IME composition, keeps Enter-to-send and Shift+Enter newline behavior, and exposes an accessible keyboard-use label | `app.html`; static UI contract and full suite 87 tests pass; exact Vercel deployment `1c7de33` READY; live DOM verified | `1c7de33` |');
  else if (task === 68) rows.push('| 68 | ✅ Complete | Workspace composer displays a concise Enter-to-send and Shift+Enter hint on desktop, with readable theme styling and a narrow-screen hide rule to protect mobile controls | `app.html`; attachment contract and full suite 88 tests pass; exact Vercel deployment `e9bd5c3` READY; live DOM text, geometry, and mobile CSS rule verified | `e9bd5c3` |');
  else if (task === 69) rows.push('| 69 | ✅ Complete | Workspace chat exposes polite, atomic live status text for thinking, completion, stop, and failure states without changing provider behavior | `app.html`; static UI contract and full suite 89 tests pass; Vercel deployment `9460654` READY | `9460654` |');
  else if (task === 70) rows.push('| 70 | ✅ Complete | Workspace chat exposes aria-busy while a generation is active and clears it on command, image, normal, stopped, and failed completion paths | `app.html`; static UI contract and full suite 90 tests pass; Vercel deployment `0e08c70` READY | `0e08c70` |');
  else if (task === 71) rows.push('| 71 | ✅ Complete | Workspace send control exposes accurate accessible names for sending and stopping a generation throughout the existing lifecycle | `app.html`; static UI contract and full suite 91 tests pass; Vercel deployment `8845c48` READY; live initial control label verified | `8845c48` |');
  else if (task === 72) rows.push('| 72 | ✅ Complete | Workspace returns keyboard focus to the composer after local command or image-request completion, only when focus remains on the send control | `app.html`; static UI contract and full suite 92 tests pass; Vercel deployment `b0b0682` READY; live workspace load verified | `b0b0682` |');
  else if (task === 73) rows.push('| 73 | ✅ Complete | Workspace stop action expands to a touch-safe 88px width only while generating, keeping its visible label readable on narrow screens | `app.html`; static UI contract and full suite 93 tests pass; Vercel deployment `e017ece` READY; live workspace load verified | `e017ece` |');
  else if (task === 74) rows.push('| 74 | ✅ Complete | Workspace restores composer focus after normal, stopped, or failed generation cleanup only when the send control remains focused | `app.html`; static UI contract and full suite 94 tests pass; Vercel deployment `af88761` READY; live workspace load verified | `af88761` |');
  else if (task === 75) rows.push('| 75 | ✅ Complete | Workspace announces “Stopping generation” immediately through the existing live status region before aborting an active request | `app.html`; static UI contract and full suite 95 tests pass; Vercel deployment `1875fa8` READY; live workspace load verified | `1875fa8` |');
  else if (task === 76) rows.push('| 76 | ✅ Complete | Mobile navigation control exposes an explicit open/close label and synchronized aria-expanded state while preserving the existing sidebar behavior | `app.html`; static UI contract and full suite 96 tests pass; Vercel deployment `8483d82` READY; live control label verified | `8483d82` |');
  else if (task === 77) rows.push('| 77 | ✅ Complete | Workspace drawer close control exposes an explicit Close workspace label while preserving the existing toggle behavior | `app.html`; static UI contract and full suite 97 tests pass; Vercel deployment `aa6778e` READY; live workspace load verified | `aa6778e` |');
  else if (task === 78) rows.push('| 78 | ✅ Complete | Workspace Files control exposes a synchronized aria-expanded state and aria-controls relationship for the existing drawer | `app.html`; static UI contract and full suite 98 tests pass; Vercel deployment `34f86eb` READY; live workspace load verified | `34f86eb` |');
  else if (task === 79) rows.push('| 79 | ✅ Complete | Keyboard users can close an open workspace drawer with Escape and return focus to the existing Files control | `app.html`; static UI contract and full suite 99 tests pass; Vercel deployment `443fa2d` READY; live workspace load verified | `443fa2d` |');
  else if (task === 80) rows.push('| 80 | ✅ Complete | Sign-in modal exposes an explicit labelled modal-dialog role with a stable Account access title | `app.html`; static UI contract and full suite 100 tests pass; Vercel deployment `b318e90` READY; live dialog title verified | `b318e90` |');
  else if (task === 81) rows.push('| 81 | ✅ Complete | Sign-in dialog moves focus to its email field when opened and returns focus to its trigger after explicit dismissal | `app.html`; static UI contract and full suite 101 tests pass; Vercel deployment `ed15260` READY; live workspace load verified | `ed15260` |');
  else if (task === 82) rows.push('| 82 | ✅ Complete | Escape closes the open sign-in dialog through its existing dismissal path and restores focus to the triggering control | `app.html`; static UI contract and full suite 102 tests pass; Vercel deployment `9464675` READY; live Escape and focus-return behavior verified | `9464675` |');
  else if (task === 83) rows.push('| 83 | ✅ Complete | Plans, usage, and settings cards expose explicit labelled modal-dialog semantics without changing existing behavior | `app.html`; static UI contract and full suite 103 tests pass; Vercel deployment `ed117b9` READY; live DOM semantics verified | `ed117b9` |');
  else if (task === 84) rows.push('| 84 | ✅ Complete | Plans, usage, and settings close controls expose specific accessible names without changing existing behavior | `app.html`; static UI contract and full suite 104 tests pass; Vercel deployment `409c9b8` READY; live DOM labels verified | `409c9b8` |');
  else if (task === 85) rows.push('| 85 | ✅ Complete | Escape dismisses an open plans, usage, or settings dialog through its existing close path without overriding earlier handlers | `app.html`; static UI contract and full suite 105 tests pass; Vercel deployment `c51fd39` READY; live plans Escape behavior verified | `c51fd39` |');
  else if (task === 86) rows.push('| 86 | ✅ Complete | Plans, usage, and settings dialogs focus their close control when opened and restore focus to the initiating control on dismissal | `app.html`; static UI contract and full suite 106 tests pass; Vercel deployment `12d66ca` READY; live plans focus lifecycle verified | `12d66ca` |');
  else if (task === 87) rows.push('| 87 | ✅ Complete | Sign-in and sign-up email and password fields expose explicit accessible names while preserving browser autofill metadata | `app.html`; static UI contract and full suite 107 tests pass; Vercel deployment `7a26b07` READY; live sign-in field labels verified | `7a26b07` |');
  else if (task === 88) rows.push('| 88 | ✅ Complete | Switching sign-in and sign-up pages moves focus to the active page’s email field without changing authentication behavior | `app.html`; static UI contract and full suite 108 tests pass; Vercel deployment `33f8980` READY; live focus switching verified | `33f8980` |');
  else if (task === 89) rows.push('| 89 | ✅ Complete | Tab and Shift+Tab focus loops remain within the open sign-in dialog without changing sign-in behavior | `app.html`; static UI contract and full suite 109 tests pass; Vercel deployment `ef28609` READY; live focus-loop behavior verified | `ef28609` |');
  else if (task === 90) rows.push('| 90 | ✅ Complete | Tab and Shift+Tab focus loops remain within the open plans, usage, or settings dialog without changing modal behavior | `app.html`; static UI contract and full suite 110 tests pass; Vercel deployment `ab68234` READY; live plans focus-loop behavior verified | `ab68234` |');
  else if (task === 91) rows.push('| 91 | ✅ Complete | Sign-in and sign-up feedback regions announce status politely and atomically | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 92) rows.push('| 92 | ✅ Complete | Pasted-content removal and owner-tools close controls expose specific accessible names | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 93) rows.push('| 93 | ✅ Complete | Owner-tools card exposes explicit labelled modal-dialog semantics | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 94) rows.push('| 94 | ✅ Complete | Owner-tools settings entry is keyboard-operable without changing owner actions | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 95) rows.push('| 95 | ✅ Complete | Owner-tools dialog moves focus to close control and restores the triggering control on dismissal | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 96) rows.push('| 96 | ✅ Complete | Escape dismisses the open owner-tools dialog through its existing close path | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 97) rows.push('| 97 | ✅ Complete | Tab and Shift+Tab stay within the open owner-tools dialog | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 98) rows.push('| 98 | ✅ Complete | Usage-limit modal exposes explicit labelled modal-dialog semantics | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 99) rows.push('| 99 | ✅ Complete | Usage-limit dialog focuses its first action and restores the triggering control on dismissal | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 100) rows.push('| 100 | ✅ Complete | Escape dismisses the open usage-limit dialog through its existing close path | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 101) rows.push('| 101 | ✅ Complete | Credit top-up modal exposes explicit labelled modal-dialog semantics | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 102) rows.push('| 102 | ✅ Complete | Credit top-up dialog focuses its first pack and restores the triggering control on dismissal | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 103) rows.push('| 103 | ✅ Complete | Escape dismisses the open credit top-up dialog through its existing close path | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 104) rows.push('| 104 | ✅ Complete | Tab and Shift+Tab stay within the open credit top-up dialog | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 105) rows.push('| 105 | ✅ Complete | Redeem-code modal exposes explicit labelled modal-dialog semantics | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 106) rows.push('| 106 | ✅ Complete | Redeem-code dialog focuses its field and restores the triggering control on dismissal | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 107) rows.push('| 107 | ✅ Complete | Escape dismisses the open redeem-code dialog through its existing close path | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 108) rows.push('| 108 | ✅ Complete | Tab and Shift+Tab stay within the open redeem-code dialog | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 109) rows.push('| 109 | ✅ Complete | Purchase-confirmation modal exposes explicit labelled modal-dialog semantics | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 110) rows.push('| 110 | ✅ Complete | Purchase-confirmation dialog focuses Continue and restores the triggering control on dismissal | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 111) rows.push('| 111 | ✅ Complete | Escape dismisses the open purchase-confirmation dialog through its existing close path | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 112) rows.push('| 112 | ✅ Complete | Tab and Shift+Tab stay within the open purchase-confirmation dialog | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 113) rows.push('| 113 | ✅ Complete | Post-purchase confetti canvas is hidden from assistive technologies as decorative content | `app.html`; full suite 134 tests pass; Vercel deployment `0e2634d` success | `0e2634d` |');
  else if (task === 114) rows.push('| 114 | ✅ Complete | Workspace model choices expose clear accessible descriptions aligned to Spark, Star, Comet, and Nova routing tiers | `app.html`; full suite 135 tests pass; Vercel deployment `914160d` success | `914160d` |');
  else if (task === 115) rows.push('| 115 | ✅ Complete | Workspace model selector exposes synchronized menu disclosure state while preserving routing and plan gates | `app.html`; full suite 136 tests pass; Vercel deployment `bac8dd8` success | `bac8dd8` |');
  else if (task === 116) rows.push('| 116 | ✅ Complete | Escape closes the open workspace model menu and restores focus to the selector trigger | `app.html`; full suite 137 tests pass; Vercel deployment `e1716bb` success | `e1716bb` |');
  else if (task === 117) rows.push('| 117 | ✅ Complete | Tab and Shift+Tab wrap within the open workspace model menu, excluding hidden or disabled choices | `app.html`; full suite 138 tests pass; Vercel deployment `748882e` success | `748882e` |');
  else if (task === 118) rows.push('| 118 | ✅ Complete | Keyboard-triggered model-menu opening moves focus to the first visible available choice while pointer opening remains unchanged | `app.html`; full suite 139 tests pass; Vercel deployment `4bb3ae1` success | `4bb3ae1` |');
  else if (task === 119) rows.push('| 119 | ✅ Complete | ArrowDown and ArrowUp move focus through visible enabled model-menu choices with wrapping | `app.html`; full suite 140 tests pass; Vercel deployment `b07c844` success | `b07c844` |');
  else if (task === 120) rows.push('| 120 | ✅ Complete | Model choices expose synchronized menu-radio selection state, while the plan action remains a menu item | `app.html`; full suite 141 tests pass; Vercel deployment `56fd574` success | `56fd574` |');
  else if (task === 121) rows.push('| 121 | ✅ Complete | Mobile model menu is scrollable and overscroll-contained within its bounded viewport, keeping all choices reachable | `app.html`; full suite 142 tests pass; Vercel deployment `3fd1a80` success | `3fd1a80` |');
  else if (task === 122) rows.push('| 122 | ✅ Complete | Permitted model selection refreshes the visible checks and radio-menu selected state immediately | `app.html`; full suite 143 tests pass; Vercel deployment `f4cf7a0` success | `f4cf7a0` |');
  else if (task === 123) rows.push('| 123 | ✅ Complete | Primary landing-page hero CTA explicitly states that the first script can be generated free while retaining the existing workspace destination | `index.html`; full suite 144 tests pass; Vercel deployment `3607ecc` success | `3607ecc` |');
  else if (task === 124) rows.push('| 124 | ✅ Complete | Keyboard visitors can skip landing-page navigation and focus the main content through a visible-on-focus skip link | `index.html`; full suite 145 tests pass; Vercel deployment `fa6d442` success | `fa6d442` |');
  else if (task === 125) rows.push('| 125 | ✅ Complete | The visible mobile landing-header theme control has a reliable 44px minimum touch target | `index.html`; full suite 146 tests pass; Vercel deployment `161edbf` success | `161edbf` |');
  else if (task === 126) rows.push('| 126 | ✅ Complete | Landing-page favorite-tool controls expose their unsaved pressed state before the existing renderer restores saved state | `index.html`; full suite 147 tests pass; Vercel deployment `f51b7ce` success | `f51b7ce` |');
  else if (task === 127) rows.push('| 127 | ✅ Complete | The landing-page feature-search result count announces existing filter updates politely to assistive technology | `index.html`; full suite 148 tests pass; Vercel deployment `e1f4d14` success | `e1f4d14` |');
  else if (task === 128) rows.push('| 128 | ✅ Complete | Every landing-page FAQ disclosure button is linked to its labelled answer region while preserving existing one-at-a-time behavior | `index.html`; full suite 149 tests pass; Vercel deployment `8a0920a` success | `8a0920a` |');
  else if (task === 129) rows.push('| 129 | ✅ Complete | Landing-page FAQ answer regions expose accessibility visibility that matches their existing open and closed state | `index.html`; full suite 150 tests pass; Vercel deployment `3b3268e` success | `3b3268e` |');
  else if (task === 130) rows.push('| 130 | ✅ Complete | The landing-page saved-tool count announces existing local favorite updates politely to assistive technology | `index.html`; full suite 151 tests pass; Vercel deployment `0385ac0` success | `0385ac0` |');
  else if (task === 131) rows.push('| 131 | ✅ Complete | Decorative landing-page FAQ plus icons are hidden from assistive technology while retaining their existing visual state treatment | `index.html`; full suite 152 tests pass; Vercel deployment `e6a7d64` success | `e6a7d64` |');
  else if (task === 132) rows.push('| 132 | ✅ Complete | Landing-page FAQ structured data includes additional answers already visible in the public FAQ, without changing the canonical page meaning | `index.html`; full suite 153 tests pass; Vercel deployment `142e107` success | `142e107` |');
  else if (task === 133) rows.push('| 133 | ✅ Complete | Workspace generation status remains announced by one authoritative live region while the duplicate visual thinking bubble is decorative | `app.html`; full suite 154 tests pass; Vercel deployment `1f1a351` success | `1f1a351` |');
  else if (task === 134) rows.push('| 134 | ✅ Complete | The existing workspace chat conversation region has a stable accessible name while preserving busy and scrolling behavior | `app.html`; full suite 155 tests pass; Vercel deployment `cf40560` success | `cf40560` |');
  else if (task === 135) rows.push('| 135 | ✅ Complete | The existing workspace message composer is exposed as a named form landmark without changing keyboard or send controls | `app.html`; full suite 156 tests pass; Vercel deployment `e2f11a3` success | `e2f11a3` |');
  else if (task === 136) rows.push('| 136 | ✅ Complete | The workspace pasted-content summary announces its existing feedback politely while retaining the visual remove control | `app.html`; full suite 157 tests pass; Vercel deployment `3dfe7db` success | `3dfe7db` |');
  else if (task === 137) rows.push('| 137 | ✅ Complete | The existing workspace sidebar is exposed as named navigation without changing its chat, account, or utility controls | `app.html`; full suite 158 tests pass; Vercel deployment `4721d8e` success | `4721d8e` |');
  else if (task === 138) rows.push('| 138 | ✅ Complete | The existing workspace chat search control has an explicit accessible name without changing filter behavior | `app.html`; full suite 159 tests pass; Vercel deployment `be7a2ad` success | `be7a2ad` |');
  else if (task === 139) rows.push('| 139 | ✅ Complete | Existing workspace chat-history heading and list have an explicit accessible relationship without changing chat behavior | `app.html`; full suite 160 tests pass; Vercel deployment `be7a2ad` success | `be7a2ad` |');
  else if (task === 140) rows.push('| 140 | ✅ Complete | The landing workspace preview is exposed as one concise labelled visual while visible hero content and CTA behavior remain unchanged | `index.html`; full suite 161 tests pass; Vercel deployment `be7a2ad` success | `be7a2ad` |');
  else if (task === 141) rows.push('| 141 | ✅ Complete | The narrow-phone landing workspace preview omits its unreadably compact decorative sidebar while preserving hero conversion paths | `index.html`; full suite 162 tests pass; Vercel deployment `be7a2ad` success | `be7a2ad` |');
  else if (task === 142) rows.push('| 142 | ✅ Complete | Workspace chat-history entries are keyboard-operable through separate chat-open and options controls without nested interactions | `app.html`; full suite 163 tests pass; Vercel deployment `be7a2ad` success | `be7a2ad` |');
  else if (task === 143) rows.push('| 143 | ✅ Complete | Existing chat Options popup exposes accurate menu semantics, trigger state, keyboard navigation, and focus return | `app.html`; full suite 164 tests pass; Vercel deployment `be7a2ad` success | `be7a2ad` |');
  else if (task === 144) rows.push('| 144 | ✅ Complete | Public landing plan actions have explicit plan-oriented accessible labels without changing prices or destinations | `index.html`; full suite 165 tests pass; Vercel deployment `be7a2ad` success | `be7a2ad` |');
  else if (task === 145) rows.push('| 145 | ✅ Complete | Workspace model chooser supports Home and End focus navigation across visible choices without changing selection, routing, or plan gates | `app.html`; full suite 173 tests pass; Vercel deployment `aae13ce` success | `aae13ce` |');
  else if (task === 146) rows.push('| 146 | ✅ Complete | Specialist workspace model choices have concise explicit accessible names that state their output focus | `app.html`; full suite 173 tests pass; Vercel deployment `aae13ce` success | `aae13ce` |');
  else if (task === 147) rows.push('| 147 | ✅ Complete | Final public free-generation call to action has an explicit accessible name without changing its destination or offer | `index.html`; full suite 173 tests pass; Vercel deployment `aae13ce` success | `aae13ce` |');
  else if (task === 148) rows.push('| 148 | ✅ Complete | Workspace recording control synchronizes its pressed state and accessible name with its existing recording mode without changing recording behavior or privacy handling | `app.html`; full suite 174 tests pass; Vercel deployment `2460d7f` success | `2460d7f` |');
  else rows.push(`| ${task} | ☐ Pending | Next approved implementation task | — | — |`);
}
rows.push('', '## Current count', '', '**148 / 400 implementation tasks complete.** The counter must not be increased because of planning, waiting, or unverified claims.', '', '## Protected boundary', '', 'Auth, payments, secrets, package files, Vercel configuration, database code, and unrelated API/server behavior remain protected unless a later task names an explicit approved change.');
writeFileSync('PROGRESS_400.md', `${rows.join('\n')}\n`);
