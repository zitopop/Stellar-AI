{
  "name": "stellar-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@vercel/analytics": "^2.0.1",
    "stripe": "^16.8.0",
    "web-push": "^3.6.7"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:syntax": "node scripts/check-source.mjs",
    "check": "npm run check:syntax && npm test",
    "build:static": "node scripts/build-static-backup.mjs",
    "deploy:safe": "node scripts/safe-deploy.mjs"
  },
  "engines": {
    "node": "22.x"
  }
}
